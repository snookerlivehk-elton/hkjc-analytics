# HKJC Analytics 升級優化報告 V2

日期：2026-07-30

## 1. 目前目標重新界定

根據最新方向，本專案的職責不再包括資料爬取。

公司端負責：

- 抓取所有賽事、馬匹、騎師、練馬師、賠率及歷史資料
- 提供結構化資料接口或資料表
- 接收本系統輸出的統計結果

你這邊負責的核心，應聚焦為一條清晰可控的計算鏈：

1. 以結構化統計資料，為每匹馬產生可解釋的基礎勝出率
2. 補充建立落敗率模型
3. 把文字型資料交由 AI 作二次加減權
4. 生成最終勝出率與落敗率
5. 把結果回傳公司系統
6. 配合即時賠率，以 Kelly Criterion 計算最佳資金比例

換句話說，系統定位應由「資料抓取分析平台」收斂為：

**統計勝率引擎 + AI 文本修正引擎 + AI 報告生成引擎 + 黃金法則控制層 + 即時投注比例引擎**

## 2. 建議的最終產品結構

建議把整體分成 5 個模組：

### 模組 A：結構化統計引擎

輸入：

- 馬匹基本資料
- 當場賽事條件
- 騎師 / 練馬師 / 檔位 / 近績 / 同程時間 / 能量等結構化數據

輸出：

- `base_win_score`
- `base_win_prob`
- `base_lose_score`
- `base_lose_prob`
- 各子因子明細

### 模組 B：AI 文本分析與修正引擎

輸入：

- 賽績指引
- 歷史競賽事件報告
- 歷史沿途走勢評述
- 模組 A 的基礎統計結果

輸出：

- `horse_briefing`
- `ai_win_adjustment`
- `ai_lose_adjustment`
- `ai_reasoning_summary`

### 模組 C：AI 報告生成引擎

輸入：

- 模組 A 的統計結果
- 模組 B 的 AI 修正結果
- 賽績指引
- 歷史競賽事件報告
- 歷史沿途走勢評述

輸出：

- `race_analysis_report`
- `reflection_report`
- `report_key_points`
- `report_risk_flags`

### 模組 D：黃金法則控制層

輸入：

- 模組 A 的統計結果
- 模組 B 的 AI 修正結果
- 模組 C 的報告摘要
- 既有黃金法則規則集

輸出：

- `golden_rule_hits`
- `golden_rule_penalties`
- `golden_rule_boosts`
- `golden_rule_decision_trace`
- `golden_rule_status`

### 模組 E：即時賠率決策引擎

輸入：

- `final_win_prob`
- 公司系統提供的即時獨贏賠率 / 位置賠率

輸出：

- `edge`
- `kelly_fraction`
- `recommended_fraction`

### 模組 F：對外回傳接口

輸出給公司系統：

- 每匹馬的基礎勝率
- AI 修正後勝率
- 基礎落敗率
- AI 修正後落敗率
- 賽事分析
- 反思報告
- 黃金法則命中與調整痕跡
- Kelly 建議資金比例
- 模型版本 / 時間戳 / 解釋字段

## 3. 正確的優化重心

這次升級不應再把精力放在：

- 爬蟲穩定性
- staging / canonical 過渡
- 外部網站資料清洗

而應專注於以下三件事：

1. **統計口徑標準化**
2. **AI 修正邏輯可控化**
3. **黃金法則規則化與可控化**
4. **投注決策輸出工程化**

## 4. 勝出率模型建議

## 4.1 模型原則

你要的不是單純排名，而是：

- 每匹馬有一個可解釋、可調參、可回測的勝出率

所以建議保留兩層：

1. `score layer`
   - 各因子先形成可解釋分數

2. `probability layer`
   - 再把分數映射成勝出率

## 4.2 建議的統計主因子

可先沿用你原本的 6 大主模組：

1. 騎／練個人評分
2. 騎練合作評分
3. 檔位評分
4. 馬匹近績評分
5. 同程完成時間評分
6. 速勢能量評分

建議先不要再加太多新因子，先把這 6 類做穩。

## 4.2A 分段時間、預計跑法與馬匹屬性標籤

由於之後會增加每匹馬每場比賽的分段時間歷史數據，這會成為非常關鍵的新能力。

這批數據不應只拿來補充完成時間，而應獨立發展成 3 層能力：

1. `分段時間特徵`
   - 從歷史 sectional times 抽出各段速度、加速、減速、末段表現、前中後段分佈

2. `預計跑法判定`
   - 根據歷史分段時間與歷史沿途位置，推斷該馬在今場更可能採取的跑法

3. `馬匹屬性標籤`
   - 若某馬在特定分段模式長期有優勢，便為其加入可解釋標籤

這會令整個系統由「只看總分」升級為：

- 不只知道哪匹馬分高
- 亦知道牠為何分高
- 更知道牠較可能如何跑這一場

## 4.2B 預計跑法的建議定義

建議先把預計跑法做成可控分類，而不是一開始就做太細。

第一版可先用以下 5 類：

1. `放頭型`
2. `前置型`
3. `跟前型`
4. `中後追型`
5. `後上型`

之後再按資料量細分。

跑法判定建議不要只看單一指標，而是綜合：

1. 早段分段速度
2. 中段守位能力
3. 末段衝刺能力
4. 歷史沿途位置
5. 場次步速環境

建議輸出：

- `predicted_runstyle`
- `runstyle_confidence`
- `runstyle_reason_codes`

## 4.2B-1 跑法判定是否需要 AI

就目前規劃而言，這一層**不建議依賴 AI 作核心判定**。

原因是：

1. 如果已具備每匹馬的同程歷史完成平均時間
2. 以及每匹馬歷史各段平均時間
3. 再加上同場各馬的完成與分段數據作相對比較

其實已經足夠做出第一版具可控性的：

- 跑法判定
- 屬性標籤判定
- 跑法信心分數

也就是說，這層應優先由統計模組完成，而不是交由 AI 推測。

## 4.2B-2 建議的判定基礎

建議以「同程歷史 + 同場相對比較」作為第一版標準。

可直接用於判定的核心依據包括：

1. 同程歷史完成平均時間
2. 同程歷史各段平均時間
3. 同場各馬之間的完成時間排序
4. 同場各馬之間的分段時間排序
5. 早、中、末段相對強弱
6. 歷史沿途位置或追位幅度

要注意的是：

這裡不建議直接用原始平均時間硬比，而應先做同條件標準化。

也就是盡量按以下條件分桶後再比較：

- 同場地
- 同路程
- 同場地狀態
- 同班次或相近班次

否則不同步速環境、不同場地條件下的原始時間會把跑法判定拉歪。

## 4.2B-3 可由統計直接判定的內容

以下內容，大部分可以直接由結構化數據判定，毋須依賴 AI：

1. `放頭型`
2. `前置型`
3. `跟前型`
4. `中後追型`
5. `後上型`
6. `末段爆發強`
7. `後上極佳`
8. `耐力特強`
9. `中段保速佳`
10. `末段易乏力`
11. `步速受限型`

## 4.2B-4 AI 在這層的角色

AI 在這層較適合做的，不是核心判定，而是：

1. 引用統計模組已判定的跑法結果
2. 引用統計模組已生成的屬性標籤
3. 把這些結果寫成每匹馬的分析簡報
4. 在賽事分析與反思報告中補充語義解釋

因此建議在會議上明確定調：

- `跑法判定 = 統計層`
- `屬性標籤 = 統計層`
- `AI 簡報 / AI 分析 / AI 反思 = 解釋層`

例如：

```json
{
  "horse_id": "H123",
  "predicted_runstyle": "後上型",
  "runstyle_confidence": 0.81,
  "runstyle_reason_codes": [
    "late_sectional_strong",
    "early_position_back",
    "closing_gain_consistent"
  ]
}
```

## 4.2C 馬匹屬性標籤建議

分段時間最有價值的地方，在於能把「馬匹風格」沉澱成長期資產。

你提到的標籤方向非常合理，例如：

- `後上極佳`
- `耐力特強`

建議把標籤系統做成標準化結構，而不只是自由文字。

第一版可先集中在以下幾類：

1. 早段能力
   - `出閘快`
   - `前段積極`
   - `放頭穩定`

2. 中段節奏
   - `中段穩定`
   - `中段保速佳`
   - `中段易失位`

3. 末段能力
   - `末段爆發強`
   - `後上極佳`
   - `末段持續力佳`

4. 耐力 / 續航
   - `耐力特強`
   - `長途續航佳`
   - `末段不易掉速`

5. 風險標籤
   - `早段搶口風險`
   - `末段乏力`
   - `步速受限型`

建議每個標籤都帶：

- `label_code`
- `label_name`
- `label_strength`
- `sample_size`
- `last_verified_at`

例如：

```json
{
  "horse_id": "H123",
  "labels": [
    {
      "label_code": "late_kick_elite",
      "label_name": "後上極佳",
      "label_strength": 0.87,
      "sample_size": 9
    },
    {
      "label_code": "stamina_elite",
      "label_name": "耐力特強",
      "label_strength": 0.74,
      "sample_size": 6
    }
  ]
}
```

## 4.2D 這層能力在整個系統的用途

分段時間、跑法與標籤，不應只存在於分析頁，而應同時服務 4 個地方：

1. 統計模型
   - 可成為新主因子或子因子

2. AI 簡報
   - AI 生成每匹馬分析簡報時，可直接引用跑法與標籤

3. 賽事分析
   - AI 可據此描述場形、放頭馬、受惠馬、被步速害死的馬

4. 反思報告
   - 賽後可檢查預計跑法是否準確、標籤是否有效

## 4.2E 建議的實作順序

這層建議分 3 個 batch 落地：

### Batch 1：分段時間特徵工程

先建立每匹馬的結構化特徵，例如：

- `early_split_index`
- `mid_split_index`
- `late_split_index`
- `closing_speed_index`
- `fade_risk_index`
- `stamina_index`

### Batch 2：預計跑法模型

根據分段時間與沿途位置，輸出：

- `predicted_runstyle`
- `runstyle_confidence`

### Batch 3：屬性標籤引擎

根據長期模式穩定命中情況，產出：

- `horse_style_labels`
- `horse_strength_labels`
- `horse_risk_labels`

## 4.2F 建議的邊界

這層能力應屬於「統計層」，不是 AI 自由發揮層。

也就是：

- 跑法判定應盡量由結構化數據產出
- 屬性標籤應由規則或統計閾值產出
- AI 可以引用這些結果
- 但 AI 不應自行發明新的核心標籤口徑

這樣之後整個系統的可控性會高很多。

## 4.3 建議計算方法

每個子模組都應統一使用以下流程：

1. 把名次轉成 points
   - 冠 10 / 亞 7 / 季 5 / 殿 4 / 第五 2 / 其餘 0

2. 轉成率化平均值
   - 避免出賽多就天然高分

3. 加入時間衰減
   - 近期表現權重高於舊資料

4. 加入 sample shrinkage
   - 樣本少時向中性值回收

5. 做標準化
   - 保證不同模組可加權合成

## 4.4 勝出率轉換

最終仍建議使用：

- `Softmax + Temperature Calibration`

理由：

1. 適合處理同場相對競爭
2. 現有專案已有基礎
3. 可用歷史賽果校準 temperature

最終形式：

`base_win_prob = softmax(score / T)`

其中 `T` 不應寫死，應由歷史回測決定。

## 5. 落敗率模型建議

你提到落敗率暫時未有統計方向，這一點非常關鍵。

不建議直接用：

- `1 - 勝出率`

因為這只是「不是第一名」的機率，並不等同你要的「落敗風險」。

## 5.1 先定義落敗率是什麼

建議會議中先選定以下其中一種口徑：

### 方案 A：非勝出率

`lose_prob = 1 - win_prob`

優點：

- 最簡單
- 可立刻落地

缺點：

- 幾乎沒有額外資訊價值

### 方案 B：高風險落敗率

定義為：

- 跑出前 3 / 前 4 / 前 5 之外的機率

例如：

- `lose_prob_top4 = P(名次 > 4)`

這種定義更貼近「大敗 / 失手風險」。

### 方案 C：模型失準風險率

定義為：

- 統計條件不利 + 文本報告負面 + 波動風險高 的綜合失敗概率

這最符合你想讓 AI 去「加減落敗率」的方向。

## 5.2 最建議的做法

短期建議：

- 先採 `方案 B`

也就是先把落敗率定義為：

- `進不了前 4` 或 `進不了前 5` 的機率

因為這樣最容易：

1. 有歷史標籤
2. 可以回測
3. 可以被 AI 做加減權

## 5.3 落敗率統計因子

可從以下方向建立：

1. 極端檔位不利
2. 同程時間落差太大
3. 最近 6 仗失速率高
4. 負面競賽事件頻密
5. 長時間未有前列表現
6. 配速不合場形

## 6. AI 文本修正層建議

這一層是整個升級中最重要的第二核心。

你的想法不是讓 AI 取代統計模型，而是：

1. AI 先分析文字資料
2. AI 為每匹馬生成分析簡報
3. AI 再獨立對每匹馬作出勝敗率加減分
4. 最後再和統計模型本身算出的基礎勝敗率綜合，得出最終輸出數據

這個方向是正確而且可控的。

## 6.1 AI 的正確定位

AI 不應直接跳過統計模型去輸出最終勝率。

AI 應負責兩件事：

1. 對文字資料做語義分析，形成每匹馬的分析簡報
2. 對統計模型未覆蓋的文字信息做補充修正

也就是先有：

`horse_briefing = AI(text_data, stats_summary)`

再有：

`ai_adjustment = AI(text_data, stats_summary)`

最後才：

`final_prob = base_prob + ai_adjustment`

## 6.2 AI 輸入建議

每匹馬提供給 AI 的材料可分三類：

1. 賽績指引
2. 歷史競賽事件報告
3. 歷史沿途走勢評述

同時應連同結構化摘要一起送入：

- 基礎勝率
- 基礎落敗率
- 主因子分解
- 近期樣本摘要

## 6.2A 每匹馬分析簡報

AI 在分析文字資料時，可同步為每匹馬生成一份簡短分析簡報，作為之後修正勝敗率的語義基礎。

這份簡報建議包含：

- 馬匹當前優勢
- 馬匹當前風險
- 場形配合度
- 近期狀態判讀
- 文字材料中的正面 / 負面訊號

建議定位為：

- 給人快速閱讀
- 給 AI adjustment 提供中間語義層
- 給賽事分析報告匯總使用

## 6.3 AI 輸出格式建議

AI 不應只吐自然語言，應同時輸出分析簡報與結構化結果：

```json
{
  "horse_id": "H123",
  "horse_briefing": {
    "summary": "整體形勢正面，但仍存在末段穩定性風險。",
    "positives": [
      "上仗受阻後仍追近",
      "同程表現穩定"
    ],
    "negatives": [
      "末段受壓反應不穩"
    ]
  },
  "win_adjustment_pp": 2.5,
  "lose_adjustment_pp": -1.0,
  "confidence": 0.74,
  "reasons": [
    "沿途受阻情況改善",
    "上仗步速不合仍追近"
  ],
  "risk_flags": [
    "末段受壓反應不穩"
  ]
}
```

其中 `pp` 建議用 percentage points。

例如：

- `+2.5pp`
- `-1.0pp`

這樣最易控。

## 6.4 AI 風控規則

必須加上硬限制：

1. AI 對勝率加減不可超過固定上限
   - 例如 ±5pp 或 ±8pp

2. AI 對落敗率加減也要有限幅

3. AI 只能在指定字段上說明原因

4. AI 修正後要再做 normalize

否則 AI 很容易把統計模型拉歪。

## 6.4A AI 與概率之間的邊界定義

這一層的最終邊界可明確定義為：

1. 統計模型先輸出每匹馬的基礎勝率與基礎落敗率
2. AI 讀取文字資料，為每匹馬生成分析簡報
3. AI 根據文字分析結果，獨立產出每匹馬的勝率加減分與落敗率加減分
4. 系統把 AI 加減分與基礎勝敗率綜合
5. 產生最終輸出數據

也就是：

`final_win_prob = base_win_prob + ai_win_adjustment + golden_rule_delta_win`

`final_lose_prob = base_lose_prob + ai_lose_adjustment + golden_rule_delta_lose`

關鍵點是：

- AI 可以影響最終概率
- 但 AI 影響方式必須是「加減分」
- AI 不應直接覆蓋統計模型原值
- AI 的文字分析與加減分要能被追溯

## 6.5 AI 生成賽事分析與反思報告

除咗做數值修正，AI 亦會根據優化後更可控的統計結果，再配合文字數據，獨立生成兩類報告：

1. `賽事分析`
   - 面向賽前或臨場決策
   - 解釋每匹馬的優勢、風險、場形適配、勝負關鍵

2. `反思報告`
   - 面向賽後檢討
   - 回顧統計模型、AI 修正、黃金法則與實際賽果之間的差異

這兩份報告不應只是純文字輸出，而應建立可回溯結構：

- 報告結論
- 支持證據
- 反證因素
- 風險標記
- 與最終概率的關聯

建議格式：

```json
{
  "horse_id": "H123",
  "analysis_summary": "統計面穩定，步速場形有利，屬正面候選。",
  "reflection_summary": "統計判斷方向正確，但 AI 對末段受阻的扣分不足。",
  "positive_factors": [
    "同程時間穩定",
    "近績回升"
  ],
  "negative_factors": [
    "外檔起步",
    "歷史末段抗壓不足"
  ],
  "risk_flags": [
    "若步速偏慢，優勢會被削弱"
  ]
}
```

報告層的定位應是：

- 為人讀
- 為賽後追責
- 為模型迭代提供語義證據

而不是直接覆蓋統計概率。

## 6.6 AI 報告與概率引擎的邊界

建議明確分開：

1. `AI 分析簡報`
   - 負責語義摘要與人類可讀解釋

2. `AI 修正值`
   - 負責勝敗率數值加減

3. `AI 報告`
   - 只負責解釋、摘要、反思

如果這兩者混在一起，之後會很難追蹤：

- 是哪段文字改變了概率
- 是哪條規則改變了結論
- 哪次反思真正帶來模型優化

所以報告層應是「解釋層」，不是「計算層」。

## 6.7 反思報告的真正用途

反思報告不只是寫給人看，還應作為下一輪優化輸入：

1. 標記哪個統計因子常常高估
2. 標記哪類文字訊號常常有效
3. 標記 AI 哪些調整過大或過小
4. 標記黃金法則哪些規則經常誤判

如果這層做得好，未來每次賽後都可以沉澱成：

- 模型修正候選
- AI prompt 修正候選
- 黃金法則規則修正候選

## 7. 黃金法則控制層建議

你提到「黃金法則」的運作仍需再優化，而且要變得可控，這點非常重要。

我建議不要再把黃金法則當成模糊經驗集合，而是升級成：

**一個可配置、可開關、可限幅、可追蹤的規則控制層**

## 7.1 黃金法則的正確定位

黃金法則不應直接取代統計模型或 AI。

它比較適合扮演：

1. 規則防呆層
2. 特殊情境加減權層
3. 風控約束層

也就是：

- 統計模型給基礎概率
- AI 做文本修正
- 黃金法則再作有限幅度的規則控制

## 7.2 黃金法則應如何變得可控

建議每條黃金法則都拆成固定結構：

1. `rule_id`
2. `rule_name`
3. `trigger_condition`
4. `scope`
5. `effect_type`
6. `effect_size`
7. `max_impact`
8. `priority`
9. `enabled`

例如：

```json
{
  "rule_id": "GR_003",
  "rule_name": "極端負面競賽事件壓制",
  "trigger_condition": "negative_event_count >= 2 and recent_form_score < 0.45",
  "scope": "lose_prob",
  "effect_type": "add_pp",
  "effect_size": 3.0,
  "max_impact": 5.0,
  "priority": 80,
  "enabled": true
}
```

## 7.3 黃金法則的可控原則

建議至少有以下 5 條控制原則：

1. 每條規則都可獨立開關
2. 每條規則都有最大影響幅度
3. 同類規則總影響要有 ceiling
4. 每次命中都要留下 trace
5. 規則結果不能覆蓋原始統計，只能做有限修正

## 7.4 黃金法則目前最適合先處理的場景

在你還未完全定稿前，黃金法則可先集中在這幾類：

1. 明顯負面競賽事件
2. 歷史沿途受阻 / 搶口 / 失位等文字訊號
3. 統計與文本嚴重背離的異常情境
4. 高風險但市場熱度過高的情況
5. AI 信心不足時的保守約束

## 7.5 黃金法則先不要做的事

在規則尚未穩定前，不建議黃金法則：

1. 直接決定最終排名
2. 一次性大幅改變勝率
3. 與 Kelly 直接耦合
4. 在沒有 trace 的情況下隱性生效

否則後續幾乎無法回測與檢討。

## 8. 最終勝率與落敗率合成方法

建議流程如下：

### Step 1：統計模型

產出：

- `base_win_prob`
- `base_lose_prob`

### Step 2：AI 修正

產出：

- `horse_briefing`
- `delta_win_pp`
- `delta_lose_pp`

### Step 3：黃金法則控制

產出：

- `golden_rule_delta_win_pp`
- `golden_rule_delta_lose_pp`
- `golden_rule_trace`

### Step 4：合成

```text
final_win_prob  = clamp(base_win_prob  + delta_win_pp + golden_rule_delta_win_pp)
final_lose_prob = clamp(base_lose_prob + delta_lose_pp + golden_rule_delta_lose_pp)
```

### Step 5：重新校準

對同場所有馬：

1. 勝出率重新 normalize 到總和 = 1
2. 落敗率按定義再做一致性校準

## 9. Kelly Criterion 建議

這部分是很適合接在你最終輸出之後的。

## 8.1 輸入

Kelly 需要的不是 score，而是：

1. 模型最終勝率 `p`
2. 市場賠率 `odds`

若用十進制賠率：

- `b = odds - 1`

Kelly 公式：

`f* = (b*p - q) / b`

其中：

- `p = final_win_prob`
- `q = 1 - p`

## 8.2 實務建議

不建議直接用 Full Kelly。

建議至少用：

- Half Kelly
- Quarter Kelly

也就是：

- `recommended_fraction = k * f*`
- `k` 建議先用 `0.25` 或 `0.5`

## 8.3 即時賠率整合方式

公司系統提供即時 odds 後，可做：

1. 每次 odds 更新時重算 edge
2. 只有當 `f* > 0` 才列為可下注
3. 若 AI / 統計信心不足，可再乘一層 confidence discount

例如：

`final_bet_fraction = Kelly * model_confidence * safety_factor`

## 8.4 建議輸出欄位

回傳公司系統時，建議至少包含：

- `final_win_prob`
- `market_odds`
- `implied_prob`
- `edge`
- `kelly_fraction_raw`
- `kelly_fraction_recommended`
- `confidence_level`

## 10. 可行性評估

## 結論

這個方向非常可行，而且比「自己還要兼顧爬蟲」健康得多。

## 原因

1. 範圍更清晰
2. 技術責任更集中
3. 模型可更快回測
4. 更容易與公司系統對接

## 難點

真正的難點不在程式，而在 3 個地方：

1. 落敗率定義
2. AI 修正幅度控制
3. 黃金法則可控化
4. Kelly 輸出與真實下注風控銜接

## 11. 建議的落地順序

### Phase 1：先定義口徑

先確定：

1. 勝率定義
2. 落敗率定義
3. AI 可修正的範圍
4. 黃金法則可調整的範圍
5. Kelly 使用 Full / Half / Quarter

### Phase 2：先完成純統計版本

先不接 AI，只做：

- 基礎勝率
- 基礎落敗率
- 回測校準

### Phase 3：加入 AI 修正層

加入：

- 文本摘要
- 結構化 AI adjustment
- 人工可審核的原因字段

### Phase 4：加入 AI 報告與反思層

完成：

- 賽事分析
- 反思報告
- 賽後可追溯證據

### Phase 5：建立黃金法則控制層

完成：

- 規則配置
- 規則限幅
- 規則 trace

### Phase 6：接公司即時賠率

完成：

- edge
- Kelly
- 推薦資金比例

### Phase 7：建立版本化回測體系

每次模型升級後都可以比較：

- 勝率準確度
- 落敗率準確度
- 黃金法則命中質量
- Kelly 實際收益表現

## 12. 我對這個方案的最終建議

最值得優先落實的，不是再擴大統計種類，而是先把以下三件事做成標準產品能力：

1. **每匹馬的基礎勝率可解釋、可校準**
2. **AI 只作有限幅度修正，而不是主導判斷**
3. **黃金法則只作有限規則控制，而不是黑盒決策**
4. **Kelly 只吃最終概率與即時賠率，不和統計層混在一起**

如果這三層切得清楚，整個系統會非常穩。

## 13. 下一步最建議產出的文件

下一步最值得先做 5 份文件：

1. `STAT_PROBABILITY_SPEC.md`
   - 定義勝率 / 落敗率 / 各統計模組 / 校準方式

2. `AI_ADJUSTMENT_SPEC.md`
   - 定義 AI 可讀文本、輸出 JSON、調整上下限、審核規則

3. `AI_REPORTING_SPEC.md`
   - 定義賽事分析 / 反思報告的輸入、輸出、格式與用途

4. `GOLDEN_RULE_ENGINE_SPEC.md`
   - 定義黃金法則的規則結構、限幅、優先級、trace 與風控邊界

5. `KELLY_INTEGRATION_SPEC.md`
   - 定義賠率欄位、edge、Kelly 計算與輸出接口

這五份一旦定稿，就可以開始正式拆 batch 開發。
